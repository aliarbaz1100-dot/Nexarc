(()=>{const ready=fn=>document.readyState==='loading'?document.addEventListener('DOMContentLoaded',fn,{once:true}):fn();ready(()=>{const player=document.querySelector('#player'),top=document.querySelector('.playerTop'),frame=document.querySelector('#gameFrame'),close=document.querySelector('#close'),retry=document.querySelector('#retry'),full=document.querySelector('#full');if(!player||!top||!frame)return;const bar=document.createElement('div');bar.className='playerPlusBar';bar.innerHTML='<span class="playerHint">ESC close · R retry · F fullscreen</span><button id="nextGame" type="button">Next Game</button>';top.appendChild(bar);const visibleCards=()=>[...document.querySelectorAll('.card')].filter(c=>c.offsetParent!==null&&!c.closest('.myListShelf'));const pickNext=()=>{const cards=visibleCards();if(!cards.length)return;const current=(location.hash||'').replace('#play-','');let i=Math.max(0,cards.findIndex(c=>c.dataset.id===current));const next=cards[(i+1)%cards.length]||cards[0];next.click()};document.querySelector('#nextGame').onclick=e=>{e.preventDefault();pickNext()};document.addEventListener('keydown',e=>{if(!player.classList.contains('open'))return;const tag=(document.activeElement?.tagName||'').toLowerCase();if(tag==='input'||tag==='textarea')return;if(e.key==='Escape'){close?.click();return}if(e.key.toLowerCase()==='r'){retry?.click();return}if(e.key.toLowerCase()==='f'){full?.click();return}if(e.key.toLowerCase()==='n'){pickNext();return}});let resizeTimer=null;const fit=()=>{clearTimeout(resizeTimer);resizeTimer=setTimeout(()=>{if(player.classList.contains('open'))document.body.classList.add('nexarcPlaying');else document.body.classList.remove('nexarcPlaying')},80)};new MutationObserver(fit).observe(player,{attributes:true,attributeFilter:['class']});fit()})})();
